<%= form_with(model: compliance_report) do |form| %>
  <% if compliance_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(compliance_report.errors.count, "error") %> prohibited this compliance_report from being saved:</h2>

      <ul>
        <% compliance_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

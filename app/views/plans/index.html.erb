<p style="color: green"><%= notice %></p>

<h1>Plans</h1>

<div id="compliance_reports">
  <% @plans.each do |plan| %>
    <%= render plan %>
    <p>
      <%= link_to "Show this compliance report", plan %>
    </p>
  <% end %>
</div>

<%= link_to "New compliance report", new_plan_path %>

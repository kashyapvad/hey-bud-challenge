<!DOCTYPE html>
<html>
<head>
  <title>AI Construction Takeoff</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <style>
    .container {
      text-align: center;
      margin-top: 50px;
    }

    .title {
      font-family: 'Courier New', Courier, monospace;
      font-size: 30px;
      font-weight: bold;
      color: #d9534f; /* Red color for the title */
      margin-bottom: 20px;
    }

    .black-bar {
      background-color: black;
      height: 50px;
      width: 100%;
      margin-bottom: 20px;
    }

    .file-upload-btn {
      background-color: #f5f5f5;
      border: 1px solid #ccc;
      border-radius: 4px;
      color: black;
      padding: 10px 20px;
      font-size: 16px;
    }

    .upload-btn {
      background-color: #5bc0de;
      color: white;
      padding: 10px 20px;
      border-radius: 4px;
      border: none;
      margin-top: 10px;
      cursor: pointer;
    }

    .upload-btn:hover {
      background-color: #31b0d5;
    }

    .status {
      margin-top: 20px;
      font-family: 'Courier New', Courier, monospace;
      font-size: 18px;
      color: black;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="title">AI CONSTRUCTION TAKEOFF</div>

    <%= form_with(model: @plan, local: true, multipart: true) do |form| %>
      <div class="form-group">
        <%= form.file_field :pdf, class: "file-upload-btn" %>
      </div>
    
      <div class="actions">
        <%= form.submit "Upload", class: "upload-btn" %>
      </div>
    <% end %>

  </div>

  <div id="compliance_reports" class="container">
    <% @plans.each do |plan| %>
      <p>
        <%= link_to plan.title, plan_path(plan) %>
        <% if plan.pdf.attached? %>
          <br>
          <%= link_to "Download PDF", rails_blob_path(plan.pdf, disposition: "attachment") %>
        <% end %>
      </p>
    <% end %>
  </div>

  <%= link_to "New compliance report", new_plan_path, class: "upload-btn" %>

</body>
</html>
